WorkflowQuery

This sample shows how to use a self-made form that executes workflow queries.

Note: 
Assure that the first parameter in line 45 is set to a valid value.
You can find valid values in the "Instance no." column in the Workflow view in TheNavigator.